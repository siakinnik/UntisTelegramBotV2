// ---------------------------------
// getTimetableForWeek.js - getTimetableForWeek function
// ---------------------------------

// main function

// const getTimetableForWeek = async (username, pass, date) => {
const getTimetableForWeek = () => {
    // siakinnik
    // I've decided not to implemet this feature, because it is, 
    // most of the time, too much information to display in one message
    return ['no', 'week'];
};

module.exports = { getTimetableForWeek };